# Optional override for the BestDuo API (defaults to Railway production).
# BESTDUO_API_BASE_URL=https://api-server-production-2c27.up.railway.app

# Discord webhook URL that receives contact-form inquiries.
# Required for /api/contact to forward messages.
# Create one in: Discord 채널 → 설정 → 연동 → 웹후크
DISCORD_CONTACT_WEBHOOK_URL=
